-запуск !
    -файл есть? (isFile) !
        -считывание файла (getFile) !
            да - запрос пароля (authorization) !
                -пароль верный !
                    -предложение выбора действия (choice)!
                        -заблокировать (lockFiles)!
                            -поиск пути, поиск пользователя (getUserName) !
                                -запуск перебора файлов!
                                    -применение команды к каждому файлу!
                                        -return 0!
                        -разблоировать (unlockFiles)!
                            -поиск пути, поиск пользователя (getUserName)!
                                -запуск перебора файлов!
                                    -применение команды к каждому файлу!
                                        -return 0    !  
                        -изменить template.tbl (changeFiles)!
                            -запись файлов в template.tbl!
                                -считывание файла (getFile)!
                                    -возврат к шагу выбора действий!
                            -удаление файлов!
                        -выход !
                            -return 0!
            -пароль неверный !
                -выход !
        нет - создание файла tbl  (init)!
            - запрос пароля и добавление его хэшированного вида в таблицу!
                -переход к шагу выбора действий!